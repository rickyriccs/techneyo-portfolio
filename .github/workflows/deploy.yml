name: Deploy React Production to Hostinger

on:
  push:
    branches:
      - production

jobs:
  build-and-deploy:
    name: Build Fresh and Deploy Dist
    runs-on: ubuntu-latest

    steps:
      - name: Checkout production branch
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '22'

      - name: Fresh install dependencies
        run: npm ci

      - name: Build React project
        run: npm run build

      - name: Confirm dist folder
        run: |
          ls -la
          ls -la dist

      - name: Deploy dist files to Hostinger public_html
        uses: easingthemes/ssh-deploy@main
        with:
          SSH_PRIVATE_KEY: ${{ secrets.HOSTINGER_SSH_KEY }}
          REMOTE_HOST: ${{ secrets.HOSTINGER_HOST }}
          REMOTE_USER: ${{ secrets.HOSTINGER_USER }}
          REMOTE_PORT: ${{ secrets.HOSTINGER_PORT }}
          SOURCE: "dist/"
          TARGET: ${{ secrets.HOSTINGER_DEPLOY_PATH }}
          ARGS: "-rlgoDzvc --delete"